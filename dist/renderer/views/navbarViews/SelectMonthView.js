import View from '../View.js';
export default class SelectMonthView extends View {
    constructor(parentElement) {
        super(parentElement);
    }
    generateMarkup() {
        return `<h1>HAHAHA</h1>`;
    }
}
